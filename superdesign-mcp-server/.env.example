# =============================================================================
# SuperDesign MCP Server Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual configuration values
# cp .env.example .env

# =============================================================================
# Core Configuration - AI Provider
# =============================================================================

# AI provider type (required: custom-api)
# Choose from: claude-api, claude-code, openai, openrouter, custom-api
AI_PROVIDER=custom-api

# API keys (required)
# Provide corresponding API keys based on selected AI provider
ANTHROPIC_AUTH_TOKEN=your-api-key-here
OPENAI_API_KEY=your-openai-key-here
OPENROUTER_API_KEY=your-openrouter-key-here

# API endpoint URL (optional, has default value)
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic

# Default model (optional, has default value)
ANTHROPIC_DEFAULT_SONNET_MODEL=glm-4.6

# Claude Code path (optional, only needed for claude-code provider)
CLAUDE_CODE_PATH=claude

# =============================================================================
# Project Configuration
# =============================================================================

# Workspace root directory (optional, default is current directory)
WORKSPACE_ROOT=/path/to/your/project

# Security mode (optional, default is strict)
# strict: Strict security checks, only allow access to files within workspace
# permissive: Relaxed mode, allow access to more files
SECURITY_MODE=strict

# =============================================================================
# Logging Configuration
# =============================================================================

# Enable file logging (optional, default is true)
ENABLE_FILE_LOGGING=true

# Log level (optional, default is info)
# Available values: debug, info, warn, error
LOG_LEVEL=info

# Log file directory (optional, default is ~/.superdesign/logs)
LOG_DIR=~/.superdesign/logs

# Log file name (optional, default is superdesign-mcp.log)
LOG_FILE_NAME=superdesign-mcp.log

# Maximum log file size in MB before rotation (optional, default is 10)
MAX_LOG_SIZE_MB=10

# Number of log files to keep during rotation (optional, default is 5)
LOG_FILE_BACKUPS=5

# Enable console logging in addition to file logging (optional, default is true)
ENABLE_CONSOLE_LOGGING=true

# =============================================================================
# Advanced Configuration (optional)
# =============================================================================

# SuperDesign prefixed environment variables (override default configuration)
# SUPERDESIGN_AI_PROVIDER=custom-api
# SUPERDESIGN_WORKSPACE_ROOT=/custom/path
# SUPERDESIGN_SECURITY_MODE=strict
# SUPERDESIGN_LOG_LEVEL=info
# SUPERDESIGN_MAX_VARIATIONS=3
# SUPERDESIGN_OUTPUT_DIR=.superdesign/design_iterations

# Development environment settings (optional)
# NODE_ENV=development

# =============================================================================
# Usage Instructions
# =============================================================================

# 1. Required variables: AI_PROVIDER + corresponding API keys
# 2. Other variables have reasonable default values and can be omitted
# 3. Installation script will automatically set: AI_PROVIDER, SECURITY_MODE, WORKSPACE_ROOT
# 4. API keys will be inherited from ~/.claude/settings.json unless overridden in .env
#
# LOGGING INFORMATION:
# - Default log file location: ~/.superdesign/logs/superdesign-mcp.log
# - Log files are automatically rotated when they reach MAX_LOG_SIZE_MB
# - Set LOG_LEVEL=debug for detailed troubleshooting information
# - To view logs: tail -f ~/.superdesign/logs/superdesign-mcp.log
# - To search logs: grep "ERROR" ~/.superdesign/logs/superdesign-mcp.log
# - Recent logs are also available via Claude Code CLI with: claude mcp get superdesign
# - Check logging status with MCP tool: get_logging_status