# SuperDesign MCP 服务器环境变量配置示例
# 复制此文件为 .env 并填入您的实际配置值

# =============================================================================
# 必填配置 - 大模型 API
# =============================================================================

# API 密钥 (必填)
# 从您的大模型提供商获取的 API 密钥
ANTHROPIC_AUTH_TOKEN=your-api-key-here

# API 端点 URL (可选，默认使用智谱AI)
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic

# 模型名称 (可选，默认使用 glm-4.6)
ANTHROPIC_DEFAULT_SONNET_MODEL=glm-4.6

# AI 提供商类型 (可选，默认为 custom-api)
AI_PROVIDER=custom-api

# =============================================================================
# 服务器配置
# =============================================================================

# MCP 服务器端口 (可选，仅在独立服务器模式使用)
MCP_PORT=3001

# 工作空间根目录 (可选)
WORKSPACE_ROOT=~/$USER/superdesign-mcp-server

# 安全模式 (可选，默认为 strict)
# strict: 严格安全检查，仅允许访问工作空间内的文件
# permissive: 宽松模式，允许访问更多文件
SECURITY_MODE=strict

# =============================================================================
# 日志和监控配置
# =============================================================================

# 启用文件日志 (可选，默认为 true)
ENABLE_FILE_LOGGING=true

# 日志级别 (可选，默认为 info)
# 可选值: debug, info, warn, error
LOG_LEVEL=info

# 启用监控 (可选，默认为 true)
MONITORING_ENABLED=true

# =============================================================================
# 性能配置
# =============================================================================

# 最大并发请求数 (可选，默认为 3)
MAX_CONCURRENT_REQUESTS=3

# 请求超时时间，单位秒 (可选，默认为 120)
REQUEST_TIMEOUT=120

# 启用压缩 (可选，默认为 true)
ENABLE_COMPRESSION=true

# 启用缓存 (可选，默认为 true)
CACHE_ENABLED=true

# 缓存过期时间，单位秒 (可选，默认为 3600)
CACHE_TTL=3600

# =============================================================================
# 不同大模型提供商的配置示例
# =============================================================================

# 智谱AI (bigmodel.cn) - 推荐
# ANTHROPIC_AUTH_TOKEN=your-bigmodel-api-key
# ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
# ANTHROPIC_DEFAULT_SONNET_MODEL=glm-4.6

# OpenAI
# ANTHROPIC_AUTH_TOKEN=sk-your-openai-key-here
# ANTHROPIC_BASE_URL=https://api.openai.com/v1
# ANTHROPIC_DEFAULT_SONNET_MODEL=gpt-4

# Anthropic Claude
# ANTHROPIC_AUTH_TOKEN=sk-ant-your-claude-key-here
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# ANTHROPIC_DEFAULT_SONNET_MODEL=claude-3-5-sonnet-20241022

# 本地 Ollama
# ANTHROPIC_AUTH_TOKEN=
# ANTHROPIC_BASE_URL=http://localhost:11434/v1
# ANTHROPIC_DEFAULT_SONNET_MODEL=llama3.1:8b

# =============================================================================
# 使用方法
# =============================================================================

# 1. 复制此文件为 .env
# cp .env.example .env

# 2. 编辑 .env 文件，填入您的实际配置
# nano .env

# 3. 启动服务器
# ./start-mcp-server.sh

# 4. 或者在命令行中直接设置环境变量
# export ANTHROPIC_AUTH_TOKEN="your-api-key-here"
# ./start-mcp-server.sh

# =============================================================================
# 云部署和Web客户端配置
# =============================================================================

# 可选: Web客户端的自定义域名
# REACT_APP_MCP_SERVER_URL=https://your-domain.com